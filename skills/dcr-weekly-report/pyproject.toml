[project]
name = "dcr-weekly-report"
version = "0.1.0"
description = "DCR/Data Cleanroom Weekly HTML Report Generator"
requires-python = ">=3.10"
dependencies = [
    "snowflake-connector-python>=3.0.0",
    "jinja2>=3.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
